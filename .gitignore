# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build/*.exe
/build/*.dmg
/build/*.deb
/build/*.AppImage

# electron
/electron/dist/
/release/

# misc
.DS_Store
*.pem
/asar_list.txt

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.factory

# local Claude Code worktrees and generated Skill mirrors
/.claude/worktrees/
/.claude/skills/

# dev server stdout/stderr captures (should never be committed)
/tmp/

# local SQLite runtime data (pi-coding-agent state store)
/data/
# Bun lockfile (project uses npm, see package-lock.json)
bun.lock

.idea/